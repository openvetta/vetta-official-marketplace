# X API

Connect Vetta to X's hosted API MCP server at <https://api.x.com/mcp> using an App-only Bearer Token.

This Ability is the direct read-only route documented by X. Account actions and user-context tools require the separate `xurl` OAuth bridge described in the [official MCP documentation](https://docs.x.com/tools/mcp).
